<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyPomodoro" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/azulFundo</item>
        <item name="colorSecondary">@color/azulEscuro</item>
        <item name="android:background">@color/azulFundo</item>
        <item name="android:textColor">@color/azulEscuro</item>
    </style>
    <style name="Theme.MyPomodoro" parent="Base.Theme.MyPomodoro" />

    <style name="TemaPreto" parent="Theme.Design.NoActionBar">
        <item name="colorPrimary">@color/pretoCorFundo</item>
        <item name="colorSecondary">@color/pretoEscuro</item>
        <item name="android:background">@color/pretoCorFundo</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="Theme.MyPomodoroPreto" parent="TemaPreto" />

</resources>